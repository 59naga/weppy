{
  "name": "@59naga/weppy",
  "version": "0.0.0",
  "main": "dist/weppy.js",
  "description": "tweaks antimatter15/weppy to UMD",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier weppy.mjs --write",
    "build": "babel weppy.mjs --out-dir dist --plugins=@babel/transform-modules-umd",
    "postversion": "git push --follow-tags"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.54",
    "@babel/core": "^7.0.0-beta.54",
    "@babel/plugin-transform-modules-umd": "^7.0.0-beta.54",
    "prettier": "^1.13.7"
  },
  "prettier": {
    "singleQuote": true
  }
}
